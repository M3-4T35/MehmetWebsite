{% extends 'base.html.twig' %}

{% block title %}Mehmet ATES | Portfolio{% endblock %}

{% block body %}
<section class="home-hero p-5 shadow-sm">
    <div class="container">
        <h1 class="display-1 fw-bold text-primary mb-3">Mehmet ATES</h1>
        <h2 class="display-6 mb-4">Étudiant en Master Ingénierie Système et Logiciel à l'Université Marie et Louis Pasteur</h2>
        
        <div class="mb-5 d-flex justify-content-center gap-4">
            <a href="https://github.com/M3-4T35" target="_blank" class="social-link" title="GitHub"><i class="bi bi-github"></i></a>
            <a href="https://www.linkedin.com/in/mehmet-ates-pro/" target="_blank" class="social-link" title="LinkedIn"><i class="bi bi-linkedin"></i></a>
        </div>

        <div class="d-flex flex-column flex-sm-row justify-content-center gap-3 px-3">
            <a href="{{ path('app_public_projects') }}" class="btn btn-primary btn-lg rounded-pill px-sm-5">
                Voir mes travaux
            </a>
            <a href="{{ path('app_cv_public') }}" class="btn btn-outline-primary btn-lg rounded-pill px-sm-5">
                Consulter mes CV
            </a>
        </div>
    </div>
</section>

<div class="container mb-5">
    <div class="row g-5">
        <!-- À propos & Langues -->
        <div class="col-lg-4">
            <h3 class="fw-bold mb-4 border-bottom pb-2">À propos</h3>
            <p class="small">
                Récemment diplômé de ma première année de Master en Informatique. Mon expertise se concentre sur l'architecture logicielle et l'optimisation des systèmes.
            </p>
            
            <h4 class="fw-bold mt-5 mb-3 h5">Langues</h4>
            <div class="mb-3">
                <div class="d-flex justify-content-between small mb-1">
                    <span>Français</span>
                    <span class="text-muted">Langue maternelle</span>
                </div>
                <div class="lang-level"><div class="lang-progress" style="width: 100%"></div></div>
            </div>
            <div class="mb-3">
                <div class="d-flex justify-content-between small mb-1">
                    <span>Anglais</span>
                    <span class="text-muted">C1+</span>
                </div>
                <div class="lang-level"><div class="lang-progress" style="width: 90%"></div></div>
            </div>
            <div class="mb-3">
                <div class="d-flex justify-content-between small mb-1">
                    <span>Turc</span>
                    <span class="text-muted">Intermédiaire</span>
                </div>
                <div class="lang-level"><div class="lang-progress" style="width: 60%"></div></div>
            </div>
            <div class="mb-3">
                <div class="d-flex justify-content-between small mb-1">
                    <span>Allemand</span>
                    <span class="text-muted">Notion</span>
                </div>
                <div class="lang-level"><div class="lang-progress" style="width: 25%"></div></div>
            </div>
        </div>

        <!-- Compétences -->
        <div class="col-lg-8">
            <h3 class="fw-bold mb-4 border-bottom pb-2">Compétences Techniques</h3>
            
            <div class="row g-4">
                <div class="col-md-6">
                    <h5 class="fw-bold h6 text-primary mb-3">Développement Logiciel</h5>
                    <div class="d-flex flex-wrap">
                        <span class="skill-badge">Java</span>
                        <span class="skill-badge">C++</span>
                        <span class="skill-badge">C</span>
                        <span class="skill-badge">Dart</span>
                        <span class="skill-badge">Flutter</span>
                    </div>
                </div>
                <div class="col-md-6">
                    <h5 class="fw-bold h6 text-primary mb-3">Data Science & Python</h5>
                    <div class="d-flex flex-wrap">
                        <span class="skill-badge">Python</span>
                        <span class="skill-badge">Jupyter Notebook</span>
                        <span class="skill-badge">Pandas</span>
                        <span class="skill-badge">Anaconda</span>
                    </div>
                </div>
                <div class="col-md-12">
                    <h5 class="fw-bold h6 text-primary mb-3">Développement Web Frontend & Backend</h5>
                    <div class="d-flex flex-wrap">
                        <span class="skill-badge">HTML5</span>
                        <span class="skill-badge">CSS3</span>
                        <span class="skill-badge">JavaScript</span>
                        <span class="skill-badge">TypeScript</span>
                        <span class="skill-badge">PHP</span>
                        <span class="skill-badge">Symfony</span>
                        <span class="skill-badge">Apache</span>
                    </div>
                </div>
                <div class="col-md-12">
                    <h5 class="fw-bold h6 text-primary mb-3">Bases de Données</h5>
                    <div class="d-flex flex-wrap">
                        <span class="skill-badge">Requêtes SQL</span>
                        <span class="skill-badge">Mongodb</span>
                        <span class="skill-badge">MySQL</span>
                        <span class="skill-badge">MariaDB</span>
                        <span class="skill-badge">PostgreSQL</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Projets à la une -->
<section class="bg-body-tertiary py-5 rounded-4 mb-5 shadow-sm">
    <div class="container">
        <div class="d-flex justify-content-between align-items-end mb-4">
            <div>
                <h3 class="fw-bold mb-0">À la une</h3>
                <p class="text-muted mb-0">Mes 3 derniers projets réalisés</p>
            </div>
            <a href="{{ path('app_public_projects') }}" class="btn btn-sm btn-outline-primary rounded-pill px-3">Tout voir</a>
        </div>
        
        <div class="row g-4">
            {% for preview in featuredProjects %}
                {% set project = preview.entity %}
                {% set media = preview.preview %}
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm project-card">
                        <div class="card-img-container" style="height: 160px;">
                            {% if media and media.filename %}
                                {% set ext = media.filename|split('.')|last|lower %}
                                {% if ext in ['jpg','jpeg','png','gif','webp'] %}
                                    <img src="{{ asset('uploads/media/' ~ media.filename) }}" alt="{{ project.title }}">
                                {% else %}
                                    <i class="bi bi-code-slash fs-1 text-primary opacity-25"></i>
                                {% endif %}
                            {% else %}
                                <i class="bi bi-code-slash fs-1 text-primary opacity-25"></i>
                            {% endif %}
                        </div>
                        <div class="card-body">
                            <h6 class="card-title fw-bold mb-1">
                                {% if app.request.locale == 'en' and project.titleEn %}
                                    {{ project.titleEn }}
                                {% else %}
                                    {{ project.title }}
                                {% endif %}
                            </h6>
                            <p class="card-text text-muted small mb-3">
                                {% if app.request.locale == 'en' and project.descriptionCourteEn %}
                                    {{ project.descriptionCourteEn|length > 80 ? project.descriptionCourteEn|slice(0, 80) ~ '...' : project.descriptionCourteEn }}
                                {% else %}
                                    {{ project.descriptionCourte|length > 80 ? project.descriptionCourte|slice(0, 80) ~ '...' : project.descriptionCourte }}
                                {% endif %}
                            </p>
                            <a href="{{ path('app_public_project_detail', {'id': project.id}) }}" class="btn btn-sm btn-primary w-100 rounded-pill">Détails</a>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12 text-center py-4">
                    <p class="text-muted">Aucun projet à afficher.</p>
                </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Mes Passions -->
<div class="container mb-5">
    <h3 class="fw-bold mb-4 text-center">Mes Passions</h3>
    <div class="row g-3">
        <div class="col-sm-6 col-md-3">
            <div class="passion-card p-4 h-100 text-center shadow-sm">
                <i class="bi bi-cpu passion-icon"></i>
                <h6 class="fw-bold">Informatique</h6>
            </div>
        </div>
        <div class="col-sm-6 col-md-3">
            <div class="passion-card p-4 h-100 text-center shadow-sm">
                <i class="bi bi-controller passion-icon"></i>
                <h6 class="fw-bold">Jeux Vidéo</h6>
            </div>
        </div>
        <div class="col-sm-6 col-md-3">
            <div class="passion-card p-4 h-100 text-center shadow-sm">
                <i class="bi bi-vr passion-icon"></i>
                <h6 class="fw-bold">Réalité Virtuelle</h6>
            </div>
        </div>
        <div class="col-sm-6 col-md-3">
            <div class="passion-card p-4 h-100 text-center shadow-sm">
                <i class="bi bi-egg-fried passion-icon"></i>
                <h6 class="fw-bold">Cuisine</h6>
            </div>
        </div>
    </div>
</div>
{% endblock %}
